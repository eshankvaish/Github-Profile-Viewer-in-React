import React from 'react';

import LogoutContainer from '../../containers/LogoutContainer/LogoutContainer';

const LogoutScene = (props) => (
    <LogoutContainer {...props} />
);
export default LogoutScene;
